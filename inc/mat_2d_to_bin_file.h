extern int
mat_2d_to_bin_file(
    char **ptr_X,
    size_t *ptr_nX,
    const char * file_name, // file being  created 
    int sz, // size of each element 
    int nR, // number of rows 
    int nC // number or columns 
    );
